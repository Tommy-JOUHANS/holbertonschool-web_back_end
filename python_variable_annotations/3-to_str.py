#!/usr/bin/env python3
"""Module that contains a type-annotated function to_str that takes
a float n as argument and returns the string representation of the float."""


def to_str(n: float) -> str:
    """Return the string representation of n as a str."""
    return str(n)
