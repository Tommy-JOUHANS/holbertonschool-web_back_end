#!/usr/bin/env python3
"""Module that contains a type-annotated function concat that takes two strings
and returns their concatenation as a string.
"""


def concat(str1: str, str2: str) -> str:
    """Return the concatenation of str1 and str2 as a string."""
    return str1 + str2
